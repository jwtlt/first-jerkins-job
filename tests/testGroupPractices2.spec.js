import { test } from "@playwright/test";

test.describe("Test Group", () => {
  test("1", async ({ page }) => {
    // Empty test body
  });

  test("2", async ({ page }) => {
    // Empty test body
  });

  test("3", async ({ page }) => {
    // Empty test body
  });
});
